info:
  name: schema
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/v1/database/schema
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
