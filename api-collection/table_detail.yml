info:
  name: inspect table
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/database/users
  auth: inherit

query:
  page: 1
  limit: 50

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
