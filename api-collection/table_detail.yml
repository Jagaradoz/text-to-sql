info:
  name: table detail
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/database/users
  auth: inherit

params:
  - type: path
    name: table_name
    value: users

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
