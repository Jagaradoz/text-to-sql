info:
  name: history
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/history
  auth: inherit

query:
  - name: limit
    value: 50

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
