info:
  name: inspect table
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/database/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
