info:
  name: generate
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/api/generate
  auth: inherit

body:
  type: json
  
body:json:
  {
    "prompt": "Show me all users from Canada"
  }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
